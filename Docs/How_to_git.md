# How was this repo made!

need to be in `/etc/nixos`

first, initialize the repository

```bash
sudo git init
```

then change the `.git/` ownership to the user:

```bash
sudo chown <user> .git/
```

